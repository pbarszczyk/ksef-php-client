services:
  php:
    image: ksef:php-8.1
    build: docker
    user: ksef
    working_dir: /srv
    volumes:
      - ./:/srv
      - composerCache:/home/ksef/.composer # cache pobranych bibliotek

volumes:
  composerCache:
